# Positive test: Migration with initContainer mode and single replica
# This should PASS validation - initContainer is safe with single replica
mode: deployment
replicaCount: 1

migration:
  enabled: true
  mode: initContainer

config:
  hostname: "test.example.com"

  storage:
    type: local

  database:
    type: sqlite

  lock:
    backend: local

  redis:
    enabled: false
