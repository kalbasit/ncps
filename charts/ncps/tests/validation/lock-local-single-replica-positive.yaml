# Positive test: Single replica with local lock backend
# This should PASS validation - single replica can use local lock
mode: deployment
replicaCount: 1

config:
  hostname: "test.example.com"

  storage:
    type: local

  database:
    type: sqlite

  lock:
    backend: local

  redis:
    enabled: false
