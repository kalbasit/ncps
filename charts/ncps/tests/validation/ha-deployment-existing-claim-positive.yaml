# Positive test: HA + Deployment + existingClaim
# This should PASS validation
mode: deployment
replicaCount: 3

config:
  hostname: "test.example.com"

  storage:
    type: local
    local:
      persistence:
        enabled: true
        existingClaim: my-existing-pvc
        accessModes:
          - ReadWriteOnce  # Doesn't matter when existingClaim is set
        size: 10Gi

  database:
    type: postgresql
    postgresql:
      host: postgres.default.svc
      password: test123

  redis:
    enabled: true
    addresses:
      - redis:6379
