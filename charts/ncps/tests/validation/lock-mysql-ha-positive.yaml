# Positive test: HA with MySQL lock backend
# This should PASS validation - HA with mysql lock and mysql database
mode: deployment
replicaCount: 3

config:
  hostname: "test.example.com"

  storage:
    type: s3
    s3:
      bucket: my-bucket
      endpoint: https://s3.amazonaws.com
      region: us-east-1
      accessKeyId: test-key
      secretAccessKey: test-secret

  database:
    type: mysql
    mysql:
      host: mysql.default.svc
      password: test123

  lock:
    backend: mysql

  redis:
    enabled: false
