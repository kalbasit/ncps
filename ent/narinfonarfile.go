// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/kalbasit/ncps/ent/narfile"
	"github.com/kalbasit/ncps/ent/narinfo"
	"github.com/kalbasit/ncps/ent/narinfonarfile"
)

// NarInfoNarFile is the model entity for the NarInfoNarFile schema.
type NarInfoNarFile struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// NarinfoID holds the value of the "narinfo_id" field.
	NarinfoID int `json:"narinfo_id,omitempty"`
	// NarFileID holds the value of the "nar_file_id" field.
	NarFileID int `json:"nar_file_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the NarInfoNarFileQuery when eager-loading is set.
	Edges        NarInfoNarFileEdges `json:"edges"`
	selectValues sql.SelectValues
}

// NarInfoNarFileEdges holds the relations/edges for other nodes in the graph.
type NarInfoNarFileEdges struct {
	// Narinfo holds the value of the narinfo edge.
	Narinfo *NarInfo `json:"narinfo,omitempty"`
	// NarFile holds the value of the nar_file edge.
	NarFile *NarFile `json:"nar_file,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// NarinfoOrErr returns the Narinfo value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e NarInfoNarFileEdges) NarinfoOrErr() (*NarInfo, error) {
	if e.Narinfo != nil {
		return e.Narinfo, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: narinfo.Label}
	}
	return nil, &NotLoadedError{edge: "narinfo"}
}

// NarFileOrErr returns the NarFile value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e NarInfoNarFileEdges) NarFileOrErr() (*NarFile, error) {
	if e.NarFile != nil {
		return e.NarFile, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: narfile.Label}
	}
	return nil, &NotLoadedError{edge: "nar_file"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*NarInfoNarFile) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case narinfonarfile.FieldID, narinfonarfile.FieldNarinfoID, narinfonarfile.FieldNarFileID:
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the NarInfoNarFile fields.
func (_m *NarInfoNarFile) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case narinfonarfile.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case narinfonarfile.FieldNarinfoID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field narinfo_id", values[i])
			} else if value.Valid {
				_m.NarinfoID = int(value.Int64)
			}
		case narinfonarfile.FieldNarFileID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field nar_file_id", values[i])
			} else if value.Valid {
				_m.NarFileID = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the NarInfoNarFile.
// This includes values selected through modifiers, order, etc.
func (_m *NarInfoNarFile) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryNarinfo queries the "narinfo" edge of the NarInfoNarFile entity.
func (_m *NarInfoNarFile) QueryNarinfo() *NarInfoQuery {
	return NewNarInfoNarFileClient(_m.config).QueryNarinfo(_m)
}

// QueryNarFile queries the "nar_file" edge of the NarInfoNarFile entity.
func (_m *NarInfoNarFile) QueryNarFile() *NarFileQuery {
	return NewNarInfoNarFileClient(_m.config).QueryNarFile(_m)
}

// Update returns a builder for updating this NarInfoNarFile.
// Note that you need to call NarInfoNarFile.Unwrap() before calling this method if this NarInfoNarFile
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *NarInfoNarFile) Update() *NarInfoNarFileUpdateOne {
	return NewNarInfoNarFileClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the NarInfoNarFile entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *NarInfoNarFile) Unwrap() *NarInfoNarFile {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: NarInfoNarFile is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *NarInfoNarFile) String() string {
	var builder strings.Builder
	builder.WriteString("NarInfoNarFile(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("narinfo_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.NarinfoID))
	builder.WriteString(", ")
	builder.WriteString("nar_file_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.NarFileID))
	builder.WriteByte(')')
	return builder.String()
}

// NarInfoNarFiles is a parsable slice of NarInfoNarFile.
type NarInfoNarFiles []*NarInfoNarFile
