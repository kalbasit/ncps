// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/kalbasit/ncps/ent/narfile"
	"github.com/kalbasit/ncps/ent/narinfo"
	"github.com/kalbasit/ncps/ent/narinfonarfile"
	"github.com/kalbasit/ncps/ent/predicate"
)

// NarInfoNarFileQuery is the builder for querying NarInfoNarFile entities.
type NarInfoNarFileQuery struct {
	config
	ctx         *QueryContext
	order       []narinfonarfile.OrderOption
	inters      []Interceptor
	predicates  []predicate.NarInfoNarFile
	withNarinfo *NarInfoQuery
	withNarFile *NarFileQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the NarInfoNarFileQuery builder.
func (_q *NarInfoNarFileQuery) Where(ps ...predicate.NarInfoNarFile) *NarInfoNarFileQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *NarInfoNarFileQuery) Limit(limit int) *NarInfoNarFileQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *NarInfoNarFileQuery) Offset(offset int) *NarInfoNarFileQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *NarInfoNarFileQuery) Unique(unique bool) *NarInfoNarFileQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *NarInfoNarFileQuery) Order(o ...narinfonarfile.OrderOption) *NarInfoNarFileQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryNarinfo chains the current query on the "narinfo" edge.
func (_q *NarInfoNarFileQuery) QueryNarinfo() *NarInfoQuery {
	query := (&NarInfoClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(narinfonarfile.Table, narinfonarfile.FieldID, selector),
			sqlgraph.To(narinfo.Table, narinfo.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, narinfonarfile.NarinfoTable, narinfonarfile.NarinfoColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryNarFile chains the current query on the "nar_file" edge.
func (_q *NarInfoNarFileQuery) QueryNarFile() *NarFileQuery {
	query := (&NarFileClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(narinfonarfile.Table, narinfonarfile.FieldID, selector),
			sqlgraph.To(narfile.Table, narfile.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, narinfonarfile.NarFileTable, narinfonarfile.NarFileColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first NarInfoNarFile entity from the query.
// Returns a *NotFoundError when no NarInfoNarFile was found.
func (_q *NarInfoNarFileQuery) First(ctx context.Context) (*NarInfoNarFile, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{narinfonarfile.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *NarInfoNarFileQuery) FirstX(ctx context.Context) *NarInfoNarFile {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first NarInfoNarFile ID from the query.
// Returns a *NotFoundError when no NarInfoNarFile ID was found.
func (_q *NarInfoNarFileQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{narinfonarfile.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *NarInfoNarFileQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single NarInfoNarFile entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one NarInfoNarFile entity is found.
// Returns a *NotFoundError when no NarInfoNarFile entities are found.
func (_q *NarInfoNarFileQuery) Only(ctx context.Context) (*NarInfoNarFile, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{narinfonarfile.Label}
	default:
		return nil, &NotSingularError{narinfonarfile.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *NarInfoNarFileQuery) OnlyX(ctx context.Context) *NarInfoNarFile {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only NarInfoNarFile ID in the query.
// Returns a *NotSingularError when more than one NarInfoNarFile ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *NarInfoNarFileQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{narinfonarfile.Label}
	default:
		err = &NotSingularError{narinfonarfile.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *NarInfoNarFileQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of NarInfoNarFiles.
func (_q *NarInfoNarFileQuery) All(ctx context.Context) ([]*NarInfoNarFile, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*NarInfoNarFile, *NarInfoNarFileQuery]()
	return withInterceptors[[]*NarInfoNarFile](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *NarInfoNarFileQuery) AllX(ctx context.Context) []*NarInfoNarFile {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of NarInfoNarFile IDs.
func (_q *NarInfoNarFileQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(narinfonarfile.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *NarInfoNarFileQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *NarInfoNarFileQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*NarInfoNarFileQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *NarInfoNarFileQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *NarInfoNarFileQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *NarInfoNarFileQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the NarInfoNarFileQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *NarInfoNarFileQuery) Clone() *NarInfoNarFileQuery {
	if _q == nil {
		return nil
	}
	return &NarInfoNarFileQuery{
		config:      _q.config,
		ctx:         _q.ctx.Clone(),
		order:       append([]narinfonarfile.OrderOption{}, _q.order...),
		inters:      append([]Interceptor{}, _q.inters...),
		predicates:  append([]predicate.NarInfoNarFile{}, _q.predicates...),
		withNarinfo: _q.withNarinfo.Clone(),
		withNarFile: _q.withNarFile.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithNarinfo tells the query-builder to eager-load the nodes that are connected to
// the "narinfo" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *NarInfoNarFileQuery) WithNarinfo(opts ...func(*NarInfoQuery)) *NarInfoNarFileQuery {
	query := (&NarInfoClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withNarinfo = query
	return _q
}

// WithNarFile tells the query-builder to eager-load the nodes that are connected to
// the "nar_file" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *NarInfoNarFileQuery) WithNarFile(opts ...func(*NarFileQuery)) *NarInfoNarFileQuery {
	query := (&NarFileClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withNarFile = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		NarinfoID int `json:"narinfo_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.NarInfoNarFile.Query().
//		GroupBy(narinfonarfile.FieldNarinfoID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *NarInfoNarFileQuery) GroupBy(field string, fields ...string) *NarInfoNarFileGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &NarInfoNarFileGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = narinfonarfile.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		NarinfoID int `json:"narinfo_id,omitempty"`
//	}
//
//	client.NarInfoNarFile.Query().
//		Select(narinfonarfile.FieldNarinfoID).
//		Scan(ctx, &v)
func (_q *NarInfoNarFileQuery) Select(fields ...string) *NarInfoNarFileSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &NarInfoNarFileSelect{NarInfoNarFileQuery: _q}
	sbuild.label = narinfonarfile.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a NarInfoNarFileSelect configured with the given aggregations.
func (_q *NarInfoNarFileQuery) Aggregate(fns ...AggregateFunc) *NarInfoNarFileSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *NarInfoNarFileQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !narinfonarfile.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *NarInfoNarFileQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*NarInfoNarFile, error) {
	var (
		nodes       = []*NarInfoNarFile{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withNarinfo != nil,
			_q.withNarFile != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*NarInfoNarFile).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &NarInfoNarFile{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withNarinfo; query != nil {
		if err := _q.loadNarinfo(ctx, query, nodes, nil,
			func(n *NarInfoNarFile, e *NarInfo) { n.Edges.Narinfo = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withNarFile; query != nil {
		if err := _q.loadNarFile(ctx, query, nodes, nil,
			func(n *NarInfoNarFile, e *NarFile) { n.Edges.NarFile = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *NarInfoNarFileQuery) loadNarinfo(ctx context.Context, query *NarInfoQuery, nodes []*NarInfoNarFile, init func(*NarInfoNarFile), assign func(*NarInfoNarFile, *NarInfo)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*NarInfoNarFile)
	for i := range nodes {
		fk := nodes[i].NarinfoID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(narinfo.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "narinfo_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *NarInfoNarFileQuery) loadNarFile(ctx context.Context, query *NarFileQuery, nodes []*NarInfoNarFile, init func(*NarInfoNarFile), assign func(*NarInfoNarFile, *NarFile)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*NarInfoNarFile)
	for i := range nodes {
		fk := nodes[i].NarFileID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(narfile.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "nar_file_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *NarInfoNarFileQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *NarInfoNarFileQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(narinfonarfile.Table, narinfonarfile.Columns, sqlgraph.NewFieldSpec(narinfonarfile.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, narinfonarfile.FieldID)
		for i := range fields {
			if fields[i] != narinfonarfile.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withNarinfo != nil {
			_spec.Node.AddColumnOnce(narinfonarfile.FieldNarinfoID)
		}
		if _q.withNarFile != nil {
			_spec.Node.AddColumnOnce(narinfonarfile.FieldNarFileID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *NarInfoNarFileQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(narinfonarfile.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = narinfonarfile.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// NarInfoNarFileGroupBy is the group-by builder for NarInfoNarFile entities.
type NarInfoNarFileGroupBy struct {
	selector
	build *NarInfoNarFileQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *NarInfoNarFileGroupBy) Aggregate(fns ...AggregateFunc) *NarInfoNarFileGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *NarInfoNarFileGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NarInfoNarFileQuery, *NarInfoNarFileGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *NarInfoNarFileGroupBy) sqlScan(ctx context.Context, root *NarInfoNarFileQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// NarInfoNarFileSelect is the builder for selecting fields of NarInfoNarFile entities.
type NarInfoNarFileSelect struct {
	*NarInfoNarFileQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *NarInfoNarFileSelect) Aggregate(fns ...AggregateFunc) *NarInfoNarFileSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *NarInfoNarFileSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NarInfoNarFileQuery, *NarInfoNarFileSelect](ctx, _s.NarInfoNarFileQuery, _s, _s.inters, v)
}

func (_s *NarInfoNarFileSelect) sqlScan(ctx context.Context, root *NarInfoNarFileQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
